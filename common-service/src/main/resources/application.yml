spring:
  application:
    name: common-service
  jpa:
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh,prometheus
  endpoint:
    health:
      show-details: always

springdoc:
  info:
    title: Common Service API
    description: Assistudy Common Service
    version: 1.0.0
  swagger-ui:
    operations-sorter: method
    display-request-duration: true
  api-docs:
    path: /v3/api-docs
